# ============================================================
# hk-stock-monitor - Python dependencies
# Core stack: pandas / TA-Lib / matplotlib / backtrader /
#             beautifulsoup4 / requests / scikit-learn
# ============================================================

# --- Data & technical analysis ---
pandas>=2.0.0
numpy>=1.24.0

# NOTE: TA-Lib requires the underlying C library (libta-lib).
# On Linux/macOS install it first, e.g.:
#   macOS  : brew install ta-lib
#   Debian : apt-get install libta-lib-dev (build from source)
#   pip    : pip install TA-Lib
# Use 0.7.x (not 0.4.x) for numpy>=2 / Python 3.12 compatibility.
#
# ⚠️ OPTIONAL: TA-Lib is NOT strictly required anymore. ta_engine.py has a
# pure-pandas fallback (SMA/Bollinger) that runs with zero compilation, so the
# whole project works on any machine even without the C library (broken Homebrew
# Mac, Windows without DLL, Vercel serverless). TA-Lib is still used when present.
# See README.md "TA-Lib installation":
TA-Lib>=0.7.1

# --- Charting (server-safe: use Agg backend) ---
matplotlib>=3.8.0

# --- Backtesting skeleton ---
backtrader>=1.9.78.123

# --- Web scraping ---
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0            # optional faster HTML parser for bs4

# --- ML feature processing (optional) ---
scikit-learn>=1.3.0

# --- Frontend: Streamlit (primary UI) ---
streamlit>=1.36.0

# --- Environment (optional; supabase_store reads env too) ---
python-dotenv>=1.0.0
pilmoji>=2.0.0  # optional nicer emoji rendering in Streamlit


# --- Supabase (used by supabase_store.py — required for persistence) ---
supabase>=2.10.0
